# ⏰ NativeLoopTimer (原生多任务定时中心)

> 🚀 **拒绝 200MB 内存的臃肿！** 专为 Windows 设计的「极轻量、超低功耗、绝对免打扰」原生多任务倒计时与智能闹钟效率利器。

---

## 🎯 为什么我们需要它？（击中痛点的“避坑指南”）

目前市面上大部分的定时器或效率工具，都存在以下让人抓狂的**致命缺陷**：

* ❌ **臃肿的 Electron 架构**：区区一个定时器，动辄打包出 **100MB+** 的体积，运行时吞噬 **200MB+** 内存！让轻薄本续航缩水，老电脑风扇狂转。
* ❌ **霸道夺取输入焦点**：你正在全屏打联机游戏、疯狂敲代码或者做 PPT，倒计时突然到期，弹窗瞬间抢走了你的鼠标和键盘光标！游戏被迫弹回桌面，正在输入的文字直接中断，体验极差。
* ❌ **假死与“睡眠遗忘症”**：电脑一旦合盖进入休眠，很多软件的计时器就直接“假死”挂掉。醒来后错过的会议、吃药提醒全部被遗漏，根本没有补发机制。
* ❌ **任务栏与托盘重影**：手抖多次双击了图标，任务栏直接多出五六个一模一样的重复进程，关也关不完，乱成一团。

---

## ✨ 它是如何“拿捏”你的使用体验的？（九大杀手级特性）

### 1. 🍃 极度克制的“低功耗原生架构”
* 拒绝套壳浏览器！采用 Python 原生 Win32 消息循环和 CustomTkinter 精心雕琢。
* 运行内存仅需 **15MB-18MB**，CPU 占用在闲置时趋近于 **0%**。相较于 Electron 打包的软件（动辄 150MB+ 内存占用），极度省电清爽！

### 2. 🤫 绝对的免打扰通知 (Zero Focus Stealing)
* 深度调用 Windows 原生 Toast 消息推送机制，配合优化的顶层绘制，通知只会在右下角静默划出。
* **绝对不会抢走你的当前焦点**。哪怕你正在全屏激烈枪战或打字，你的输入流也完全不会被中断！如果不小心误触了通知，程序也会在后台静默继续，毫无干扰。

### 3. 🛌 聪明的“休眠唤醒补偿” (Power-State Smart Compensation)
* 深度集成 Windows 电源管理，监听系统级休眠与唤醒事件（`WM_POWERBROADCAST`）。
* 当电脑合盖休眠、再次醒来时，系统会**立刻判定并毫秒级补发**所有休眠期间错过的提醒，并以唤醒时间为基准，智能重跑新一轮倒计时。

### 4. 📈 动态渐变卡片进度条 (Dynamic Progress Bar)
* 每个任务卡片下方均嵌入一条极细、优雅的 CustomTkinter 进度条。
* 进度条根据剩余时间比例实时平滑更新，并伴随时间推移进行**三色自适应过渡**：
  * 🟢 **薄荷绿 (`#10B981`)**：时间充裕（$\ge 60\%$）
  * 🟡 **琥珀金 (`#F59E0B`)**：倒计时过半（$20\% - 60\%$）或处于**暂停状态**
  * 🔴 **警示红 (`#EF4444`)**：临近结束（$< 20\%$ 或不足 1 分钟），提供极其直观的视觉交互。

### 5. 📌 📌 Mini-Window 迷你悬浮画中画模式 (PiP Mode)
* 点击主窗体右上角的“钉子”按钮，主窗体瞬间隐藏，弹出一个极其精致、无边框、半透明（`alpha=0.85`）的迷你悬浮窗（200x70 px）于屏幕右下角。
* 支持鼠标点击任意区域进行**全桌面自由拖拽**，完美悬浮于你的游戏或 IDE 编码界面一角而不遮挡工作区。
* 悬浮小窗集成了独立的 **▶/⏸** 播放暂停、**🔄** 重置及 **↩** 还原主窗口按钮，状态实时 ticking 倒计时同步。

### 6. 🔔 自定义闹铃音效选择器 (Sound Ringtone Picker)
* 新建计时器/闹钟时支持下拉选择多款免版权的 Windows 经典预设音效（“经典闹铃”、“晨光风铃”、“静谧和弦”、“温馨叮咚”等）。
* 纯本地 Windows 音频库支持（`winsound` 驱动），支持高频切换的零延迟预览。
* 完美解决重叠音痛点：通过重构 XML 模板强行静音了系统的默认提示音，让您选择的个性化铃声在滑出通知时独家、同步、完美释放。

### 7. 🔄 手动作业控制（Start/Pause/Reset）
* 每个任务卡片均配备专用的**播放/暂停**和**重置（🔄）**按钮。
* 您可以随时手动对任意的倒计时进行初始化重置、暂停或继续，完全自主掌握节奏。

### 8. 🌐 中英双语自适应一键切换 (Dynamic Bilingual Support)
* 界面右上角配备常驻玻璃自适应语言切换按钮（`EN` / `中`）。
* 点击后，无需重启软件，系统菜单、任务表单、输入占位符、弹窗、正在运行的卡片状态、甚至是滑出的 Windows 通知全部实时刷新为对应的语言，对全球用户完全无痛。

### 9. 🎹 极致高效的物理输入框 (Physical Auto-Tabbing)
* 闹钟输入采用**物理分割的小时、分钟独立输入域**。
* **极速盲打体验**：输入满 2 位数自动跳转到下一格，敲击方向键或退格键实现无缝回退，输入效率比传统下拉滑动选择框高出十倍！

---

## 🎨 现代高颜值界面 (Harmonious Modern Aesthetics)

* **极客暗黑风**：基于精挑细选的和谐暗黑 HSL 色彩（深灰、幽蓝、薄荷绿与琥珀金组合），长时间盯盘不伤眼。
* **自适应滚动容器**：任务卡片无限添加？界面底层采用全局滚动布局，右侧的滚动条只有在内容溢出时才会悄悄浮现，未溢出时自动消失，视界纯粹而极致。
* **常驻后台托盘**：点击“开始”后，主窗口彻底消失，只在右下角系统托盘常驻一个小图标，支持右键直接全局暂停、恢复和进入设置。

---

## 📦 如何下载与使用？

### 🚀 极速启动（推荐）
1. 前往本仓库的 [Releases](https://github.com/thisisfrankryan/NativeLoopTimer/releases) 页面。
2. 下载打包好的单文件可执行程序 `NativeLoopTimer.exe`。
3. 双击直接运行即可，无需安装，无广告，无多余依赖。

### 💻 开发者自编译
如果你希望基于源码自行修改和编译：
```bash
# 1. 克隆仓库
git clone https://github.com/thisisfrankryan/NativeLoopTimer.git
cd NativeLoopTimer

# 2. 安装核心依赖
pip install customtkinter pystray winrt-Windows.UI.Notifications winrt-Windows.Data.Xml.Dom pywin32

# 3. 运行项目
python main.py

# 4. 自行打包 exe
pyinstaller NativeLoopTimer.spec --noconfirm
```

---

## 🛠️ 开源协议 (License)

本项目采用 [MIT License](LICENSE) 开源协议。你可以自由地使用、修改、再分发甚至用于商业项目。

---

* 💡 **如果有帮到你的日常效率，欢迎在右上角点一个 ⭐️ Star 给予作者支持！**
* 📬 **遇到问题或有更好的想法？欢迎随时开 [Issues](https://github.com/thisisfrankryan/NativeLoopTimer/issues) 提交反馈！**
